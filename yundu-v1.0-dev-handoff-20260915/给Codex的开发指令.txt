请根据本交付包实现“云渡文件交换平台”V1.0。

首先完整读取 01-documents 下的 Word 开发定稿，作为唯一产品与系统设计基线；结合“开始阅读.txt”理解包内配套资源和已知验证状态。不要根据旧对话、旧版本文档或文件名推断额外需求。

请在当前项目建立合理的工程结构、AGENTS.md、实施计划、OpenAPI 契约及 MySQL 版本迁移，按照文档 M0～M9 阶段持续推进开发。先完成可启动的工程、身份与权限，再完成双向文件交换、可编排审批、管理员代办、对象存储、Proxy、群通知、Zabbix 只读接口、审计及后台管理。

技术栈使用 Go + React/TypeScript + Ant Design + MySQL + MinIO/S3；模块化单体与内部持久任务，不增加终端 Agent、Redis、ClickHouse、消息中间件或微服务。界面统一使用“云渡文件交换平台”，标语为“生产与办公网络文件交换及审批”，使用 02-brand 中的 SVG 资源。

办公到生产仅允许 SQL 和 CSV，单文件最大 30 MiB、最多 5 个且每单总量不超过 150 MiB；逐字节检查真实大小、完整 UTF-8、控制字节和已知二进制特征，不做 SQL 语义/语法或 CSV 内容/结构审查。默认不开启杀毒，不伪造扫描通过；文件只交换、不执行。原始文件名和对象 UUID key 分开保存。

本地密码和 Authenticator TOTP 登录、审批版本冻结、管理员代办真实操作者审计、跨域与下载授权、资源槽位、任务幂等和故障恢复均按文档实现。企业微信第一版只提供群机器人 @ 处理人，通过配置的网络 Proxy 出站。

实现 Zabbix 7.0 HTTP 只读监控适配器，与 03-monitoring 模板字段一致：配置采集源白名单、签发只读 Token、启用后使用 Bearer 认证。禁止 Token 获取附件、审批或修改配置。先实现真实持久聚合，再验证模板导入、触发和恢复；响应示例中的固定时间和数值不可作为生产常量。

根据约 30 人在线和 60 人短时峰值执行容量与故障测试。常规可逆实现选择自行完成，遇到真实网络、密钥或企业环境缺失时记录明确的待联调项目；不要上传真实业务文件或凭据到第三方，也不要在未授权的企业群自动发测试消息。

交付须有可运行代码、迁移、开发部署环境、配置说明、真实功能测试和 AC01～AC64 验收矩阵；不得用静态页面、占位适配器、假成功响应或虚构压测结果代替实现。完成后总结已实现范围、测试证据、启动步骤和剩余待联调事项。
